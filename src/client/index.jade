extends layout

block content
    link(href="stylesheets/login.css" rel="stylesheet")
    link(href="stylesheets/util.css" rel="stylesheet")
    .blocTop
        h1= title
    //p.bottom #{subtitle}

    .limiter
         .wrap-login100
                 form(id="login_form", method="post", action="/").validate-form
                    .wrap-input100.validate-input(data-validate='Enter username')
                        input.input100(type='text', name='username', placeholder='Username')
                        span.focus-input100()
                    .wrap-input100.validate-input(data-validate='Enter password')
                        input.input100(type='password', name='pass', placeholder='Password')
                    .container-login100-form-btn.m-t-32
                        button.login100-form-btn#btnLogin
                            | Login

    .limiter_text
        .wrap-text20
            h5 MoveOn is a research project that aim at supporting movement learning from video. This project has been funded by the diagonale de Paris-Saclay, the ANR project ELEMENT and the ERC CREATIV.
                .copyright.line Copyright 2019 - Rivière Jean-Philippe All Rights Reserved



    script.
      document.getElementById("login_form").addEventListener("submit", formSubmitted);
          function formSubmitted() {
            var session_username_dom = document.getElementsByName('username');
            console.log(session_username_dom[0].value);
            var session_username = session_username_dom[0].value;

            sessionStorage.setItem('session_username', session_username);

          }
    //script(src="./src/client/js/utilities/session.js")


    //window.addEventListener("click", screenTouched, false);
        function screenTouched() {
          window.location.href = '/moveon'
        }
    //window.addEventListener("touchstart", screenTouched, false);
        function screenTouched() {
          window.location.href = '/moveon'
        } */

